# Project configuration

This folder contains non-sensitive configuration files for environments, source tables, pipeline parameters, and processing rules.

Do not store passwords, access keys, connection strings, or other secrets here.